# Required by every recipe
UNSILOED_API_KEY=your-unsiloed-api-key

# Recipe-specific (only the recipe that uses them needs the matching key set)
ANTHROPIC_API_KEY=your-anthropic-api-key   # claude/tool-use
OPENAI_API_KEY=your-openai-api-key         # openai/cli
